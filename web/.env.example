# The base URL of the JobPilot FastAPI backend. No trailing slash.
# For Vercel: set this on the project's Environment Variables page.
# For local dev: copy to `.env.local` alongside this file.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
